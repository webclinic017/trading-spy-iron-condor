{
  "$schema": "https://schema.claudedesktop.com/mcp.json",
  "mcpServers": {
    "trading-ui": {
      "command": "python",
      "args": ["dashboard/mcp_server.py", "--port", "8765"],
      "description": "Trading Dashboard Component Manifest - exposes design tokens, components, and patterns for AI-assisted UI development",
      "cwd": "${CLAUDE_PROJECT_DIR}"
    },
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--headless"],
      "description": "Playwright MCP for browser automation - sentiment scraping and trade verification"
    },
    "alpaca": {
      "command": "uvx",
      "args": ["alpaca-mcp-server"],
      "description": "Official Alpaca MCP Server (alpacahq/alpaca-mcp-server) - tools for US equities, options, and market data",
      "env": {
        "APCA_API_KEY_ID": "${ALPACA_API_KEY}",
        "APCA_API_SECRET_KEY": "${ALPACA_SECRET_KEY}",
        "APCA_PAPER": "true"
      }
    },
    "rss": {
      "command": "npx",
      "args": ["-y", "@veithly/rss-mcp"],
      "description": "RSS feed reader for US equities market news and analysis",
      "env": {
        "RSS_FEEDS": "https://feeds.marketwatch.com/marketwatch/topstories"
      }
    },
    "youtube-transcript": {
      "command": "npx",
      "args": ["-y", "@sinco-lab/mcp-youtube-transcript"],
      "description": "YouTube transcript extractor - get transcripts and metadata from YouTube videos"
    },
    "pal": {
      "command": "npx",
      "args": ["-y", "@anthropic/pal-mcp-server"],
      "description": "PAL MCP Server - Provider Abstraction Layer for multi-model orchestration. Challenge tool for adversarial validation, Debug tool for systematic investigations",
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
        "PAL_ENABLED_TOOLS": "challenge,debug,chat",
        "PAL_DEFAULT_MODEL": "openrouter/anthropic/claude-sonnet-4"
      }
    },
    "vertex-ai": {
      "command": "bunx",
      "args": ["vertex-ai-mcp-server"],
      "description": "Vertex AI MCP Server - Access Gemini models, manage training jobs, and enable web search grounding",
      "env": {
        "GOOGLE_CLOUD_PROJECT": "${GOOGLE_CLOUD_PROJECT}",
        "GOOGLE_CLOUD_LOCATION": "us-central1",
        "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}"
      }
    },
    "trunk": {
      "url": "https://mcp.trunk.io/mcp",
      "type": "http",
      "description": "Trunk CI Autopilot MCP Server - Merge Queue, Flaky Tests detection, CI auto-fixing"
    },
    "perplexity": {
      "command": "npx",
      "args": ["-y", "@perplexity-ai/mcp-server"],
      "description": "Perplexity MCP Server - Real-time web search and market intelligence for trading decisions",
      "env": {
        "PERPLEXITY_API_KEY": "${PERPLEXITY_API_KEY}"
      }
    }
  }
}
