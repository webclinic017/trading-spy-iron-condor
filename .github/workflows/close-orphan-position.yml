name: Close Orphan Position

# Uses close_position() API - the recommended way to close positions
# Updated Jan 22, 2026

on:
  workflow_dispatch:
    inputs:
      target_symbol:
        description: "Symbol to close (e.g., SPY260220P00658000)"
        required: true
        default: "SPY260220P00658000"
      dry_run:
        description: "Dry run only"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
  push:
    paths:
      - "triggers/CLOSE_ORPHAN.trigger"

# CRITICAL: Global trade concurrency - prevents race conditions (LL-281)
concurrency:
  group: global-trade-execution
  cancel-in-progress: false

env:
  SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

jobs:
  close-position:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.target_symbol != '' || github.event_name == 'push' }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install alpaca-py

      - name: Close Orphan Position
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_PAPER_TRADING_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_PAPER_TRADING_API_SECRET }}
          PAPER_TRADING: "true"
          TARGET_SYMBOL: ${{ github.event.inputs.target_symbol || 'ALL' }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: |
          if [ "$TARGET_SYMBOL" = "ALL" ]; then
            echo "Closing ALL option positions..."
            python3 scripts/close_all_options.py
          else
            echo "Closing specific symbol: $TARGET_SYMBOL"
            python3 scripts/close_orphan_put.py
          fi

      - name: Summary
        if: always()
        run: |
          echo ""
          echo "=========================================="
          echo "CLOSE ORPHAN POSITION SUMMARY"
          echo "=========================================="
          echo "Target Symbol: ${{ github.event.inputs.target_symbol || 'SPY260220P00658000' }}"
          echo "Dry Run: ${{ github.event.inputs.dry_run || 'false' }}"
          echo "=========================================="
