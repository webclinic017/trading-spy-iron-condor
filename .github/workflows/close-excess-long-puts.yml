name: Close Excess Long Puts

# Emergency workflow to close the imbalanced SPY260220P00658000 position
# Jan 22, 2026: 8 long vs 2 short = 6 excess contracts bleeding money
# SCHEDULED: Runs every 15 min during market hours until position closes

on:
  workflow_dispatch:
  # DISABLED Jan 23, 2026 - Crisis mode ended, new $30K account
  # schedule:
  #   - cron: '*/15 14-20 * * 1-5'

# Prevent concurrent trade execution (LL-281 race condition fix)
concurrency:
  group: global-trade-execution
  cancel-in-progress: false

permissions:
  contents: write

env:
  SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

jobs:
  close-excess:
    name: Close Excess Long Puts
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install alpaca-py

      - name: Close Excess Long Puts
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_PAPER_TRADING_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_PAPER_TRADING_API_SECRET }}
        run: python scripts/close_excess_long_puts.py

      - name: Trigger State Sync
        if: success()
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/sync-alpaca-status.yml/dispatches" \
            -d '{"ref": "main"}'
