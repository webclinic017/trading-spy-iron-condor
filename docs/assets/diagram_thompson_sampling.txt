Thompson Sampling Learning Curve: How the System Gets Smarter Over Time

A scientific visualization showing how Thompson Sampling reduces uncertainty with more data.

The diagram has two main panels side by side:

Left panel — "Early (10 signals)":
A Beta distribution curve with alpha=3, beta=2. The curve is wide and spread out, showing high uncertainty. The x-axis is "Estimated Success Rate" from 0% to 100%. The y-axis is "Probability Density." The peak of the distribution is around 60% but the 95% confidence interval spans from 15% to 95%. A shaded region shows this wide confidence interval. Text annotation: "alpha=3, beta=2 | E[p]=60% | High uncertainty — could be anywhere from 15% to 95%"

Right panel — "Late (81 signals)":
A Beta distribution curve with alpha=63, beta=18. The curve is tall and narrow, showing low uncertainty. The peak is around 78%. The 95% confidence interval spans from 68% to 86%. A shaded region shows this narrow confidence interval. Text annotation: "alpha=63, beta=18 | E[p]=78% | Confident — success rate is between 68% and 86%"

Between the two panels, a horizontal arrow labeled "More feedback signals" connects them.

Below both panels, a formula box:
"P(success) ~ Beta(alpha, beta)
E[success rate] = alpha / (alpha + beta)
Each thumbs-up: alpha += 1 | Each thumbs-down: beta += 1
30-day exponential decay keeps recent signals relevant"

The style should be scientific, like a textbook figure. Clean white background, professional axis labels, blue curves with light blue shaded confidence intervals.