CI/CD Pipeline: 30+ Automated Quality Gates

A vertical flowchart showing the complete CI/CD pipeline that validates every code change.

Layout: Top-to-bottom flow, with parallel branches for independent checks.

TRIGGER (top):
- Pull Request opened or Push to main
- Arrow pointing down to parallel gate groups

GATE GROUP 1 - CODE QUALITY (left column, blue):
- Lint & Format (ruff check + ruff format)
- Type Check (pyright strict mode)
- Syntax & Import Verification
- Dead Code Detection (max 50 unreferenced)
- Label: "Code Quality Gates"

GATE GROUP 2 - SECURITY (center-left column, red):
- GitGuardian Secret Scanning
- CodeQL Static Analysis
- Socket Security (dependency audit)
- Credential Validation (no hardcoded secrets)
- Label: "Security Gates"

GATE GROUP 3 - TESTING (center-right column, green):
- Unit Test Suite (1300+ tests with coverage)
- Safety Gate Tests (drawdown, slippage, assumptions)
- Safety Matrix Tests (multi-config validation)
- Agent Handoff Gate (AGENTS.md contract)
- Label: "Test Gates"

GATE GROUP 4 - TRADING SPECIFIC (right column, orange):
- Core Strategy Metric Validation
- Safe Order Wrapper Enforcement
- North Star Cadence KPI Alert
- Workflow Integrity Check
- Label: "Trading Safety Gates"

MERGE GATE (bottom):
- All 4 groups must pass (AND gate)
- Arrow to "Squash Merge to main"
- Then auto-deploy to GitHub Pages

SELF-HEALING LOOP (side annotation):
- "If CI fails → Auto-fix workflow → Re-run"
- Shows circular arrow back to trigger

Stats annotation:
- "30+ checks per PR"
- "100% green requirement"
- "Zero drift policy"

Style: Professional flowchart, dark theme, color-coded gate groups.
